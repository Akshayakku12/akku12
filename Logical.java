public class Logical {
    public static void main(String[] args) {
        int A = 10;
        int B = 20;
        boolean result = (A > B) && (A != B); // Fixed the logical expression
        System.out.println("(A > B) && (A != B): " + result); // Adjusted the output message
    }
}
