public class Vowels {
    public static void main(String[] args) {
        char ch = 'r'; // Corrected the character declaration
        if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') { // Fixed the 'if' condition
            System.out.println("It is a Vowel");
        } else {
            System.out.println("It is not a Vowel");
        }
    }
}
